const multiplicationNumber = 7;

for (let i = 1; i <= 10; i++) 
    {
        console.log(`${multiplicationNumber} x ${i} = ${multiplicationNumber * i}` )
    }

let j = 1;
while (j <= 10)
    {
        console.log(`${multiplicationNumber} x ${j} = ${multiplicationNumber * j}` )
        j++;
    }